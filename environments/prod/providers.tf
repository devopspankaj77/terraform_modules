terraform {
  required_version = ">= 1.0"
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

provider "azurerm" {
  features {}
  # Use ARM_SUBSCRIPTION_ID environment variable or Azure CLI default. Do not hardcode subscription IDs in code.
}
